import Link from "next/link";

const Navbar = () => {
  return (
    <header className="w-full px-3 flex items-center justify-between h-14 shadow">
      <div>{/* TODO: We will add logo here */}</div>
      <nav>
        <ul>
          <li></li>
        </ul>
      </nav>
    </header>
  );
};

export default Navbar;
